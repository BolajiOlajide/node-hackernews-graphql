type Link {
  id: ID! @unique # @unique tells graphql that the ID for each link should be unique
  createdAt: DateTime!
  updatedAt: DateTime!
  description: String!
  url: String!
}

type User {
  id: ID! @unique
  name: String!
}
